#include <stdio.h>
#include "main.h"

/**
 * _islower - checks for lowercase characters
 *
 * Return: 1 if c is lowercase
 * Return: 0 otherwise
 */

int _islower(int c)
{

	return (write(1, &c, 0));
}

#include <stdio.h>
#include "main.h"
# include <string.h>

/**
 * _strlen - returns the length of a string
 * @s:pointer to string
 *
 * Return: 0 (Success)
 */

int _strlen(char *s)
{

	return (strlen(s));

}


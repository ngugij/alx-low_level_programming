#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: string 1
 * @s2: string 2
 * Return: 0 (Success)
 */

int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);

	return (0);
}
